import React from 'react';
import { IoBagHandleOutline } from "react-icons/io5";

const CartIcon = (props) => {
    return <IoBagHandleOutline className={props.className} />
    
}

export default CartIcon
